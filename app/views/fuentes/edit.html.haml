.mantform
  %h2
    Editar #{Fuente.human_name}
  - semantic_form_for @fuente do |form|
    = form.semantic_errors
    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(fuentes_path)
