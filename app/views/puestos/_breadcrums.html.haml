#breadcrumbs.span-24.last
  %ul#crumbs
    %li
      = link_to t("breadcrums.homepage"), main_index_path
    %li
      = link_to t("breadcrums.puestos"), puestos_path
    - if @forview
      - if @forview == "NEW"
        %li= t("puestos.title_new")
      - elsif @forview == "EDIT"
        %li= t("puestos.title_edit")
      - else
        %li= t("puestos.title_show")
.clear &nbsp;
