.mantform
  %h2
    Editar #{Documentocategoria.human_name}
  - semantic_form_for @documentocategoria do |form|
    = form.semantic_errors
    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(documentocategorias_path)
