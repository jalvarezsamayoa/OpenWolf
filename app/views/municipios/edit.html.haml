- @forview = "EDIT"
= render :partial => "breadcrums"
.mantform
  %h2
    Editar #{Municipio.human_name}
  - semantic_form_for @municipio do |form|
    = form.semantic_errors
    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(municipios_path)
