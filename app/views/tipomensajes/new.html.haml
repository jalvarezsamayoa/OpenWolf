.mantform
  %h2
    Nuevo #{Tipomensaje.human_name}
  - semantic_form_for @tipomensaje do |form|
    = form.semantic_errors
    = render :partial => 'form', :locals => {:form => form}
    = buttons_create(tipomensajes_path)
